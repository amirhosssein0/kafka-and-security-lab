apiVersion: v1
kind: Service
metadata:
  labels:
    app: web
  name: my-np-svc
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
    nodePort: 30080
  selector:
    app: web
  type: NodePort


